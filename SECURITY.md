# Security Policy

Do not commit production secrets. Override `Jwt__SigningKey` through environment variables outside local development.

## Local development
The repository contains a development-only signing key so the demo starts without external services or API keys.

## Reporting
Open a private security advisory or contact the repository owner. Do not publish exploitable details before a fix is available.
