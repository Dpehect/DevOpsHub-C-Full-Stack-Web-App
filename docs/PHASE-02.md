# Phase 02 — Authentication and Security

Implemented: registration, login, JWT access token, rotating refresh token, logout, current user endpoint, SQLite persistence, secure password hashing, validation, seeded admin, protected React session and responsive authentication UI.
